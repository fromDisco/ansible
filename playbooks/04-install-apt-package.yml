---
- name: Installing an apt package
  hosts: all

  tasks:
    - name: install wireshark from apt
      apt: 
        name: wireshark
        state: present
    become: true
    become_user: michel
