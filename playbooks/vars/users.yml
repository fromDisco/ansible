---
users:
  - name: ansible
    password: "1234"
    groups: 
      - name: sudo
    create_home: true
    state: present
    shell: /bin/bash
    ssh_pub_key_location: /root/.ssh/id_ed25519.pub

  - name: ghost
    password: "!"
    create_home: true
    state: present
    shell: /bin/bash
    ssh_pub_key_location: /root/.ssh/id_ed25519.pub
